---
type: lint
date: 2026-04-27
ingest_count_at_lint: 4
---

# Lint findings — after ingest 4

Run after seed ingests 1–4 (Karpathy gist → 51 deck → ESMA MiCA → Circle transparency).

Vault state at lint:
- 41 wiki pages: 1 vendor, 17 concepts, 4 sectors, 4 sources, 2 people, 5 firms.
- 4 source ingests + 1 schema iteration commit (this lint may produce one more).

## Findings

### F1 — Forward-reference wiki-links to upcoming-ingest pages (informational, not a defect)

The following wiki-links currently point to pages that don't exist yet. They are forward references to ingests 5–8:

- `[[bitgo]]`, `[[fireblocks]]` — custody vendor (ingest 7).
- `[[chainalysis]]` — compliance vendor (ingest 8).
- `[[sky-protocol]]` — DeFi-native stablecoin issuer (ingest 5).
- `[[nydfs]]` — regulator (ingest 6).
- `[[bitlicense]]` — concept page (ingest 6).
- `[[qualified-custodian]]`, `[[mpc-key-management]]`, `[[bankruptcy-remote-segregation]]` — concepts deferred to ingest 7.
- `[[regulator-nydfs-bitlicense-list]]` — source page (ingest 6).
- `[[bma]]` — Bermuda Monetary Authority firm page; referenced in body of `circle.md`; deferred until a second source touches it.

**Verdict**: not a defect. Forward references are expected during incremental ingest. They will resolve as ingests 5–8 land. The lint will re-flag these only if they remain unresolved at the post-ingest-8 lint pass.

**Action**: none for now. Re-check at the after-ingest-8 lint.

### F2 — `[[source-page-name]]` is an illustrative placeholder, not a broken link — FIXED

In `wiki/concepts/citation-discipline.md` line 32:

> "the system claims X, [[source-page-name]] cites X via this verbatim quote."

This is intentional — the wiki-link syntax inside the example illustrates what a real citation would look like. A naive grep treats it as a broken link.

**Verdict**: not a defect, but the literal `[[source-page-name]]` syntax is misleading. The pedagogical example would be clearer if it used a real page (e.g. `[[karpathy-llm-wiki-gist]]`) or italic plain text (e.g. *<source-page>*).

**Action**: FIXED in this lint commit. Replaced with `[[karpathy-llm-wiki-gist]]` as a real example reference.

### F3 — Three orphan pages (no inbound wiki-links) — FIXED

The following pages exist but no other page links to them:

- `andrej-karpathy` — anchor-only person page; the source page citing the gist references the gist's source page by name, not the author by wiki-link. Author wiki-links are typically thin.
- `esma` — regulator firm page; the source page mentions ESMA in body but does not wiki-link the firm page.
- `european-commission` — anchor-only firm page; not linked from any source.

**Verdict**: minor defect. Author / publisher firm pages should be linked from the source pages they author. The wiki-link discipline says first-mention should be wiki-linked.

**Action**: FIXED in this lint commit. Added inbound wiki-links from source pages:
- `karpathy-llm-wiki-gist.md` → links `[[andrej-karpathy]]` in Cross-references.
- `regulator-eu-mica-esma-hub.md` → links `[[esma]]` and `[[european-commission]]` in Cross-references.

**Note on the lint-script false positive**: an initial pass also flagged `karpathy-llm-wiki` as orphan, but this was a `comm -23` artifact from a prefix-match collision with `karpathy-llm-wiki-gist`. The concept page in fact has 17 inbound wiki-links and is one of the most-linked pages in the vault. Lint script needs precise word-boundary matching for future runs.

### F4 — `firm.kind` enumeration not formalized; `asset-manager` is ad-hoc — FIXED

The `firm.md` template's frontmatter lists `kind: regulator | audit-firm | parent-company | consulting-firm | other`. BlackRock was assigned `kind: asset-manager`, which is not in the enumeration.

Three options considered:
1. Expand the template enumeration to include `asset-manager` (and possibly `bank`, `custodian-firm`, `ratings-agency` for future ingests).
2. Drop the enumeration discipline; treat `kind:` as free-form.
3. Use `kind: other` and capture the role in the body.

**Verdict**: schema-iteration candidate. The wiki is producing real firm-kind variety; the template should expand to match.

**Action**: FIXED in this lint commit. Expanded `_templates/firm.md` enumeration to:
`regulator | audit-firm | asset-manager | bank | custodian-firm | parent-company | consulting-firm | ratings-agency | other`

Added a "Firm kinds" subsection to `CLAUDE.md` documenting the canonical enumeration and the `other` escape-hatch rule (`other` permitted but must be paired with a body-level role explanation; new `kind:` values graduate to canonical via a schema-iteration commit after lint surfaces ≥2 vendors using the same ad-hoc value).

### F5 — Vendor `public_status:` field is starting to need richer structure

The Circle vendor page populates `public_status:` with three entries that mix two different relationship types:

- `"NYDFS-Money-Transmitter (Circle Internet Financial, LLC)"` and `"NYDFS-VCBA (Circle Internet Financial, LLC)"` — direct vendor-side licenses (Circle is licensed).
- `"BMA-Digital-Asset-Business (Circle International Bermuda Limited)"` — direct subsidiary license.

Plus a structural note in the body about Circle's USDXX → SEC-2a-7 relationship via BlackRock, which is **not** a Circle license but a reserve-vehicle relationship.

For one vendor, the flat list works (with parenthetical legal-entity qualifiers). For multiple vendors with cross-jurisdictional licensing chains, the field will accrete complexity.

**Verdict**: defer schema iteration. The current shape works for one vendor; revisit at the after-ingest-8 lint when 2–3 more vendors are populated.

**Action**: tracked for the after-ingest-8 lint.

### F6 — Substantive negative finding (Circle's missing MiCA disclosure) is well-captured but warrants a `negative-finding` cross-reference type

Circle's transparency page does NOT mention MiCA. This is a structurally significant absence — for an institutional buyer doing EU-jurisdiction diligence, it's actionable information. Currently captured as:

- An open question on the [[circle]] vendor page.
- An open question on the [[e-money-token]] concept page.
- A cross-reference note on the [[vendor-circle-transparency-page-2026-04]] source page.
- A pointer in the source page's "Cross-references" section to [[regulator-eu-mica-esma-hub]] with the framing "the EU regulatory frame that Circle does NOT mention on its transparency page; this absence is itself a finding."

**Verdict**: well-captured. The current cross-reference discipline (note in source page + open question on relevant entity pages) handles the negative finding adequately. No structural defect.

**Action**: none. Continue the pattern when future negative findings surface.

### F7 — Sector page "open questions" sections are accumulating real questions worth tracking

The four sector pages each have "Open questions" sections with substantive questions (DeFi-native vendor handling on stablecoin-issuers, on-chain custodian modeling on custody, etc.). These are accumulating but not surfaced anywhere in the index.

**Verdict**: deferred. At ~40 wiki pages this is fine; at ~100 pages a `_open-questions.md` aggregation file might be worth adding.

**Action**: no action; tracked for later.

## Path A leakage check

Run: `grep -rE "Trust Score [0-9]\.[0-9]|95-datapoint|95 datapoint|1300\+|500\+ datapoint|pillar weight" wiki/`

**Result**: All matches are negative-context disclaimers in source-page banners (the "this wiki does NOT reproduce…" paragraphs). Zero positive reproductions of proprietary 51 product output.

**Verdict**: PASS.

## Schema iteration proposal (Phase 8)

Two changes to apply in this lint commit:

1. **Expand `firm.md` template's `kind:` enumeration**. New full set: `regulator | audit-firm | asset-manager | bank | custodian-firm | parent-company | consulting-firm | ratings-agency | other`. Plus a CLAUDE.md sentence noting the enumeration is canonical and `other` is allowed only with a body-level role explanation.
2. **Fix the `[[source-page-name]]` placeholder in citation-discipline.md** by using `[[karpathy-llm-wiki-gist]]` as a real example.

Plus the orphan-page fixes from F3:

3. Add `[[andrej-karpathy]]` wiki-link from `karpathy-llm-wiki-gist.md`'s author field reference.
4. Add `[[esma]]` wiki-link from `regulator-eu-mica-esma-hub.md`'s body / publication-author area.
5. Add `[[european-commission]]` wiki-link similarly.

These five changes produce one schema-iteration commit titled `schema: iterate after ingest-4 lint`.

## Summary

- 7 findings total. 4 informational (F1, F2 follow-up, F6, F7), 3 actionable (F3 orphans, F4 firm.kind enumeration, F5 deferred for after-ingest-8).
- Path A leakage: PASS.
- Substantive concept count (≥1 substantive citation): 17 concepts. Citation-discipline at 3 sources, e-money-token at 2 sources, others single-source-substantive.
- Vendor count: 1 (Circle). Pending 7 more across ingests 5–8 (Sky + custody-vendor + Chainalysis = 3 vendors; plus implicit cross-references when ingests reveal them).
- Forward references will resolve through ingests 5–8; lint will re-check at post-ingest-8.
